import React from 'react';
import {Stack} from "@mui/material";

function HomePage(props) {
    return (
        <Stack spacing={2}>
            hello
        </Stack>
    );
}

export default HomePage;